# === Core Framework ===
fastapi[standard]==0.115.4        # Web framework chính (REST API)
uvicorn[standard]==0.32.0  # Server chạy FastAPI (ASGI server)

# === Database Layer ===
SQLAlchemy==2.0.44         # ORM kết nối và thao tác với PostgreSQL
psycopg2-binary==2.9.11    # Driver PostgreSQL cho Python
alembic==1.17.0            # Migration tool cho SQLAlchemy
greenlet==3.2.4            # Hỗ trợ threading nội bộ của SQLAlchemy
typing_extensions==4.15.0  # Đảm bảo tương thích type hint
asyncpg==0.30.0



# === Template / Utility Layer ===
Mako==1.3.10               # Dùng bởi Alembic để render file migration
MarkupSafe==3.0.3          # Hỗ trợ Mako và Jinja2 escape HTML

# === Validation & Env ===
pydantic==2.9.2            # Validate dữ liệu request/response
python-dotenv==1.0.1       # Đọc file .env để lấy biến môi trường
pydantic-settings==2.6.1   



# === Security ===
passlib[bcrypt]==1.7.4     # Hash & verify password
python-jose[cryptography]==3.3.0  # JWT token encode/decode cho auth

# === HTTP Client ===
httpx==0.28.1              # Client-side HTTP (dùng test hoặc agent gửi report)

# === Background Tasks / Scheduling (tuỳ chọn) ===
APScheduler==3.10.4        # Scheduler nếu bạn cần job chạy định kỳ

# === Dev & Testing Tools ===
pytest==8.3.3              # Unit testing framework
pytest-asyncio==0.23.8     # Dành cho test async FastAPI
requests==2.32.3           # Dùng khi test API hoặc gọi external service

# === Code Quality (optional but recommended) ===
black==24.10.0             # Code formatter tự động
isort==5.13.2              # Sort import gọn gàng
flake8==7.1.1              # Check lỗi code style

# === Logging (optional) ===
loguru==0.7.2              # Logging hiện đại, dễ dùng hơn logging mặc định
